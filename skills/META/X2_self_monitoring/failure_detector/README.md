# Failure Detector (ccos-failure-detector-v1)

This META-level self-monitoring skill acts as the OS immune system.

It detects:

- schema violations
- contradictions
- hallucinations
- infinite loops
- unsafe outputs
- truth verification failures

Outputs include:

- valid (boolean)
- issues (list)
- requires_fallback
- requires_hitl
